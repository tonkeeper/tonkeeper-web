name: Staging Deploy

on:
    push:
        branches:
            - main

# Latest push wins; older in-flight deploys cancel.
concurrency:
    group: staging-deploy
    cancel-in-progress: true

jobs:
    web-build:
        uses: ./.github/workflows/web-build.yaml
        with:
            environment: staging
        secrets: inherit
