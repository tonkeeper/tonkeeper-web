import styled from 'styled-components';

export const ProSettingsMainButtonWrapper = styled.div`
    position: sticky;
    bottom: 0;
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: 1rem 0;
    margin-top: auto;
    width: 100%;
    background-color: ${p => p.theme.backgroundPage};
`;
